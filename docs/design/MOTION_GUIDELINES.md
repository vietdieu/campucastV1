# CommuteCast Motion Guidelines (Sprint D)
*Status: Initial Audit*

- **Guideline**: All animations MUST utilize `transform` and `opacity` properties to prevent layout shifts.
- **Standard**: Follow `src/foundation/tokens/motion.ts` for all duration and easing definitions.
